<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %><br />
    <%= f.label :country %><br />
    <%= f.select :country_id,  options_from_collection_for_select(Country.all, "id", "name") ,{ :prompt => "-- Select Country --"} %><br />
    <div id="versionsDiv">
	  <%= render :partial => 'states', :locals => { :object => @states } %>
	</div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
